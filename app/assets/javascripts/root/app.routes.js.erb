jimjam.config(['$routeProvider', function ($routeProvider) {
  $routeProvider
    .otherwise({
      templateUrl: "<%= asset_path("downloads/home.html") %>",
      controller: 'DownloadHomeCtrl',
    })
}])

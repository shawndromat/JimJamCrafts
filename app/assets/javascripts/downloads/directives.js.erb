angular.module('Downloads.directives', [])

  .directive('codeGenerator', function() {
    return {
      controller: 'DownloadCodeFormCtrl',
      restrict: 'E',
      scope: { pattern: '=' },
      templateUrl: "<%= asset_path('downloads/form.html') %>"
    }
  })
